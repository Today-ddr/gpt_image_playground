## v0.8.1（2026-08-16）

### 新增
- 新增「工具」工作区与午后茶海报工作流：支持解析订单、审查餐品摆放、批量生成海报，并持久化会话记录。
- 支持先解析菜单、后补贴餐品图；贴图不再清空已解析的订单结果。
- 支持 Docker 后台生图任务、任务恢复，以及在 GitHub Actions 预构建镜像，方便低内存服务器部署。
- 设置页支持一键分享并导入全部中转站配置。
- 餐品摆放新增「图钉 / 全框」切换：图钉可直接拖动定位，全框用于查看完整标题区域。

### 变更
- 图钉模式只显示编号，不再展开成文字框；位置以图钉中心为准，可拖到图片边缘。
- 适配移动端摆放界面：窄屏隐藏重复标题和提示，放大切换按钮，并为边缘图钉留出可点空间。
- Service Worker 只缓存应用壳和静态资源，避免把任务接口和结果文件离线缓存覆盖。
- 优化午后茶工作流界面、重试和并发生图；解析阶段关闭 thinking，加快菜单分析。

## v0.7.0（2026-07-14）

### 新增
- 新增大体积备份分片导出与导入：当备份数据较大时，自动拆分为多个 ZIP 文件；恢复时一次选择同一批次的全部分片即可，选择顺序不限。导入前会检查分片是否完整，避免导入不完整或不属于同一批次的备份。(PR #118)

### 变更
- 改进数据导入导出体验：处理期间锁定设置窗口，避免误操作中断；存在正在运行或等待恢复的任务时，暂不允许导入或导出任务数据。
- 改进导入导出错误提示，并在控制台记录详细错误信息，方便排查问题。
- 优化设置页数据管理区域的说明文字、复选框和危险操作按钮样式。

### 贡献
- 感谢 @4Aiur 在 PR #118 中反馈 Firefox 大体积备份导出失败的问题，并提供测试结果和解决思路。
